replicaCount: 2

service:
  type: ClusterIP
  port: 8443

postgres:
  host: sso-pgsql-master-dev
  credentials:
    secret: sso-pgsql-dev
  admin:
    secret: sso-admin-dev

jgroupCluster:
  secret: sso-jgroups-dev

tls:
  enabled: true
