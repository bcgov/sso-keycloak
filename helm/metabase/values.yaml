##The first part of the file was autogenerated by `helm create metabase`
## The fields at the bottom were created from the metabase deployment config

nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local


## These are the values that were needed by the Metabase helm templates generated by template2helm
## Note The ones above are created by the `healm create metabase`.
metabaseImage: docker-remote.artifacts.developer.gov.bc.ca/metabase/metabase:latest
metabaseServiceName: metabase
postgresqlDatabase: metabase
postgresqlImageNamespace: openshift
postgresqlPassword: '' # Generated in the make file
postgresqlUser: metabase
postgresqlVersion: "9.6"
postgresqlVolumeCapacity: 1Gi
namespace: ""
